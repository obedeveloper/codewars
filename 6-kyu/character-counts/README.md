<h2><a href=https://www.codewars.com/kata/56b409febccd5aafbd000021/train/javascript target="_blank">Character counts</a></h2><h3>6 kyu</h3><h1 id="character-counts">Character counts</h1><p>The object is to count the number of occurances of a specified character (or set of characters) in a string.</p><h2 id="instructions">Instructions</h2><p>Complete the placeholder function <code>characterCount</code>.</p><ul><li>it should return the number of times a single character appears in the string, or</li><li>if multiple characters are specified (either by providing an array or string of length 2 or more), it should return an array of character counts</li><li>see the unit tests provided for a more comprehensive definition</li></ul><p>NOTE: The tests assume that if no arguments are provided to the function (i.e. <code>''.characterCount()</code>), then the result is <code>undefined</code>.</p>